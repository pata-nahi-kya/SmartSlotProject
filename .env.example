# ==============================================================================
# SmartSlot Project Unified Environment Configuration Template
# Copy this file to '.env' at the root, and also copy specific variables to:
#   - './SmartSlot.API/.env' for the Backend
#   - './smartslot-ui/.env' for the Frontend
# ==============================================================================

# ==============================================================================
# FRONTEND CONFIGURATION (smartslot-ui)
# ==============================================================================
# The base URL of the backend API. Vite will only expose variables starting with VITE_ to the client app.
VITE_API_URL=http://localhost:5113/api

# ==============================================================================
# BACKEND CONFIGURATION (SmartSlot.API)
# ==============================================================================
# Environment setting for ASP.NET Core
ASPNETCORE_ENVIRONMENT=Development

# Database Connection String (SQL Server)
# Change 'localhost\\SQLEXPRESS' to your SQL Server instance name if different
ConnectionStrings__DefaultConnection=Server=localhost\\SQLEXPRESS;Database=SmartSlotDb;Trusted_Connection=True;TrustServerCertificate=True;

# JWT Authentication Settings
# IMPORTANT: The secret key must be at least 32 characters long.
Jwt__Key=YOUR_SECURE_JWT_SECRET_KEY_MUST_BE_AT_LEAST_32_CHARACTERS
Jwt__Issuer=SmartSlotAPI
Jwt__Audience=SmartSlotClient
Jwt__ExpireDays=7

# CORS Allowed Origins (Vite frontend defaults to port 5173)
Cors__AllowedOrigins__0=http://localhost:5173
Cors__AllowedOrigins__1=http://127.0.0.1:5173

# Security Settings
Security__AllowAdminSelfRegistration=true
Security__RequireHttps=false
Security__MaxLoginAttemptsPerMinute=20
